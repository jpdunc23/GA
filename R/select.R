#' Identify the most important variables for prediction
#'
#' @param x An input matrix.
#' @param y A response vector.
#' @return A list of the features of \code{X} that best describe \code{y}.
#' @examples
#' add(1, 1)
#' add(10, 1)
select <- function(X, y, num_iter=100) {
  return(y)
}
